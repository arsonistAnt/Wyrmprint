<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/comic_strip_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/back_drop">

        <com.example.wyrmprint.ui.browse.comicpager.ComicStripView
            android:id="@+id/comic_strip"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:customScaleType="autoScaleWidth" />

        <ProgressBar
            android:id="@+id/comic_strip_loader"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:visibility="gone" />

        <include layout="@layout/reader_bottom_sheet_layout"/>

    </androidx.coordinatorlayout.widget.CoordinatorLayout>
</layout>
